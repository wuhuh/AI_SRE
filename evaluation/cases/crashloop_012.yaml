id: crashloop_012
fault_type: pod_crashloopbackoff
service: config-service
expected_root_cause: Pod is crashing repeatedly
expected_evidence:
  - pod_restarts
  - crashloop_event
  - startup_error_log
