route:
  receiver: 'aisre-control-plane'
  group_by: ['alertname', 'service']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: 'aisre-control-plane'
    webhook_configs:
      - url: 'http://control-plane:8080/api/v1/alerts'
        send_resolved: true